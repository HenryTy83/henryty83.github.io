/ this is a comment /
start_up:
    mov [!hardware_int], &[$3fee]
    mov $0020, d
    mov $00, mb
    mov $b000, acc

main:
    jmp &[!main]
    hlt

hardware_int:
    mov &[$8800], x
    mov &x, d
    mov $00ff, acc
    and d, acc
    mov acc, d
    mov $00, mb
    mov &[$4000], y
    mov $8000, acc
    add acc, y
    mov d, &acc
    inc y
    mov y, &[$4000]
    mov d, &x
    rti